/** @type {import('tailwindcss').Config} */
module.exports = {
  mode: "jit",

  purge: ["./assets/**/*.js", "./templates/**/*.html.twig"],
  theme: {
    extend: {},
  },
  plugins: [],
};

// These paths are just examples, customize them to match your project structure
